{
  "name": "anole",
  "version": "1.0.0",
  "description": "Anole practice.",
  "keywords": [
    "anole",
    "practice"
  ],
  "main": "index.js",
  "scripts": {
    "lint": "eslint lib test",
    "test": "mocha --require should --timeout 5000 --reporter mocha-better-spec-reporter --bail --check-leaks --recursive test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ipluser/anole.git"
  },
  "author": "ipluser",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ipluser/anole/issues"
  },
  "homepage": "https://github.com/ipluser/anole#readme",
  "devDependencies": {
    "eslint": "^3.3.1",
    "eslint-config-airbnb-base": "^5.0.2",
    "eslint-plugin-import": "^1.13.0",
    "ghooks": "^1.3.2",
    "mocha": "^3.0.2",
    "mocha-better-spec-reporter": "^3.0.2",
    "should": "^11.1.0"
  },
  "config": {
    "ghooks": {
      "pre-commit": "npm run lint && npm test"
    }
  }
}
